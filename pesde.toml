name = "patextreme/ptah_libs"
version = "0.4.0"
description = "Ptah Playbooks: the shared Luau workflow library (stdlib helpers + workflow playbooks) for ptah consumers, taken as a pesde git dependency."
license = "MIT"
repository = "https://github.com/patextreme/ptah-libs"

includes = ["lib.luau", "std/**", "playbooks/**", "README.md", "LICENSE", "pesde.toml"]

[target]
environment = "luau"
lib = "lib.luau"